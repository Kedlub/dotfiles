abbr "cz"="chezmoi"
abbr "cza"="chezmoi apply"
abbr "czcd"="chezmoi cd"
abbr "czd"="chezmoi diff"
abbr "czra"="chezmoi re-add"
abbr "czs"="chezmoi status"
abbr "czu"="chezmoi update --apply"
abbr "dcd"="docker compose down"
abbr "dcl"="docker compose logs -f"
abbr "dcp"="docker compose pull"
abbr "dcu"="docker compose up -d"
abbr "dcup"="docker compose up"
abbr "duh"="dua -x i"
abbr "ga"="git add"
abbr "gaa"="git add --all"
abbr "gb"="git branch"
abbr "gba"="git branch --all"
abbr "gci"="git commit"
abbr "gcm"="git commit -m"
abbr "gco"="git checkout"
abbr "gd"="git diff"
abbr "gds"="git diff --staged"
abbr "gl"="git log --oneline --decorate --graph --all"
abbr "gp"="git pull --ff-only"
abbr "gps"="git push"
abbr "grb"="git rebase"
abbr "grbi"="git rebase --interactive"
abbr "gst"="git status --short --branch"
abbr "gstash"="git stash push"
abbr "gstashp"="git stash pop"
abbr "gsw"="git switch"
abbr "gswc"="git switch --create"
abbr "jxe"="sudo journalctl -xe"
abbr "pcd"="podman compose down"
abbr "pcl"="podman compose logs -f"
abbr "pcp"="podman compose pull"
abbr "pcu"="podman compose up -d"
abbr "pcup"="podman compose up"
abbr "ports"="ss -tulpn"
abbr "scu"="systemctl --user"
abbr "scud"="systemctl --user disable --now"
abbr "scue"="systemctl --user enable --now"
abbr "scul"="systemctl --user daemon-reload"
abbr "scur"="systemctl --user restart"
abbr "scus"="systemctl --user status"
abbr "tsa"="tailscale status"
